{
  "algorithm": "prime_sieve",
  "input_dir": "inputs",
  "output_dir": "outputs",
  "inputs": [
    {
      "name": "ranges",
      "type": "TEXT",            // es un archivo de texto
      "mode": "SINGLE_FILE",     // un solo fichero
      "separator": "\n",         // separador de líneas
      "pattern": "*.txt"         // opcional: filtramos solo archivos txt
    }
  ]
}
